#include <iostream>
using namespace std;

int main()
{
    char ch;
    cin >> ch;
    int x = (int)ch;
    cout << x; //gives the ascii value of the character, typecasting is also used here.
    return 0;
}