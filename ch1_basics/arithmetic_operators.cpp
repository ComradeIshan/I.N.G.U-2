#include <iostream>
using namespace std;

int main() {
    int x = 7, y = 2;
    cout << x / y << endl; // do integers ke beech operation hoga to ans bhi integer aaega. isliye 3.5 ke jagah 3 aayega
    float a = 5, b = 2;
    cout << a / b << endl; //float is used for decimal numbers (more good definition is this data type is used to store real numbers)
}
