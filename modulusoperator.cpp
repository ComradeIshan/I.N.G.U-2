#include <bits/stdc++.h>
using namespace std;

int main () {
    int x = -5;
    int y = 3;
    cout << x % y << endl; //modulus operator gives the remainder of the division of two numbers. here 5/(-3) is -1 and the remainder is 2 so the answer will be 2
}